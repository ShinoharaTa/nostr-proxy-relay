# 実装計画・タスクリスト

`やりたいこと.md` に基づく実装計画です。

## フェーズ1: リレー管理と安定性向上 (優先度: 高)
リレーの接続安定性と可視化を最優先に行います。

- [ ] **リレー接続状況の監視機能 (Backend)**
    - [ ] 各リレーへの接続状態を定期チェックするバックグラウンドタスクの実装 (1分間隔)
    - [ ] 過去30分間のステータス履歴を保持する仕組みの実装
- [ ] **リレー接続状況APIの実装 (Backend)**
    - [ ] フロントエンドから接続状況履歴を取得するAPIエンドポイントの作成
- [ ] **リレー接続状況UIの実装 (Frontend)**
    - [ ] Uptime Kumaのようなステータスバー表示コンポーネントの実装
    - [ ] 1分ごとの自動更新
- [ ] **自動再接続機能 (Backend)**
    - [ ] 切断されたリレーへの自動再接続ロジックの実装
- [ ] **NIP-11情報表示 (Frontend/Backend)**
    - [ ] リレー追加・編集時にNIP-11情報を取得するAPIの実装
    - [ ] 取得した情報をモーダルで表示するUIの実装

## フェーズ2: 統計・ログ機能の強化 (優先度: 中)
運用の透明性を高めるためのログと統計機能を強化します。

- [ ] **詳細ログ記録機能 (Backend)**
    - [ ] npub + kind での Reject ログを詳細に記録するように修正
    - [ ] ログ検索・フィルタリングのためのクエリパラメータ対応
- [ ] **ログ閲覧UIの改善 (Frontend)**
    - [ ] ログのフィルタリングUI (種別、npub、キーワード等) の実装
- [ ] **統計データの収集と可視化**
    - [ ] 全POST数、Reject数の時系列データの集計ロジック実装 (Backend)
    - [ ] 統計グラフ表示コンポーネントの実装 (Frontend)
- [ ] **InfluxDB連携 (Backend)**
    - [ ] InfluxDB接続設定 (URL, Bucket, Token等) の追加
    - [ ] 流量、レイテンシ、接続数などのメトリクス送信処理の実装

## フェーズ3: REQフィルタリング設定の簡易化 (優先度: 中)
DSLを使わずに直感的に設定できるUIを提供します。

- [ ] **簡易BAN設定のデータモデル設計 (Backend)**
    - [ ] DSLとは別に、構造化されたBANルールを保存するテーブル/設定の設計
    - [ ] 簡易設定を内部的にDSLあるいはフィルタロジックに変換する仕組みの検討
- [ ] **簡易BAN設定APIの実装 (Backend)**
    - [ ] npub, kind, タグ条件などのCRUD API
- [ ] **簡易BAN設定UIの実装 (Frontend)**
    - [ ] 設定画面のタブ化 (簡易設定 / 高度なDSL設定)
    - [ ] npub BAN, kind BAN, 組み合わせ条件の入力フォーム実装
    - [ ] タグ条件設定の実装

## フェーズ4: アーキテクチャ改善・その他 (優先度: 低〜随時)
- [ ] **PostとReqの処理分離**
    - [ ] 現在のWebSocket処理フローの調査
    - [ ] Post (EVENT) と Req (REQ) の処理パイプラインの明確な分離
- [ ] **UI/UXのブラッシュアップ**
    - [ ] OpenSats審査に向けた見た目の改善、説明文の充実

## 運用確認項目 (WebSocket安定化)

以下の項目はWebSocket Keep-Alive/再接続/購読復元機能の実装後に確認すべき事項です。

### 切断検知テスト
- [ ] クライアントのネットワークを遮断した場合、120秒以内に `connection_logs.disconnected_at` が更新されること
- [ ] クライアントが正常にCloseした場合、即座に `disconnected_at` が更新されること
- [ ] 接続数カウント (active_connections) がゾンビセッションで肥大化しないこと

### バックエンドリレー再接続テスト
- [ ] バックエンドリレーを一時停止→再起動した場合、プロキシが自動的に再接続すること
- [ ] 再接続後、クライアントのタイムラインが自動的に復帰すること (REQ再送の確認)
- [ ] 再接続中にクライアントからのEVENT送信がエラーにならないこと (バッファリングまたは適切なエラー応答)

### リレープール死活監視テスト
- [ ] 管理APIの `/api/relay-status` が正確な接続状態を返すこと
- [ ] リレー切断時に `Connected → Disconnected → Connecting` の遷移がログで確認できること
- [ ] 長時間運用後も接続状態が正確であること (30分以上の連続運用)

### パフォーマンス確認
- [ ] Ping/Pong通信がメトリクス (InfluxDB等) に悪影響を与えないこと
- [ ] 多数のクライアント接続時にPing処理がボトルネックにならないこと
